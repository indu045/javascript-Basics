"use strict" ; //treat all JS code as newer version

// alert(3+3) // can't do as we are using nodejs, not browser

// primitive data type (predefined datatypes)
let name = "komal" //string
let age = 22 //number(it's range 2 to the power 53 if still can't able to store in the memory use "bigInt")
let isLoggedIn = false //boolean -true , false
let surname = null // null is a standalone value
let courseName; //undefined - abhi value define nhi hui h
//symbol => to find uniqueness use this

//non-primitive (user defined)
//object

console.log(typeof null); //object
console.log(typeof undefined); //undefined
